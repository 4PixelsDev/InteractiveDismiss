//
//
//

import Foundation

enum Constant {
    enum Animation {
        static let defaultDuration: TimeInterval = 0.30
    }

    enum Style {
        static let standardCornerRadius: CGFloat = 20
    }
}
